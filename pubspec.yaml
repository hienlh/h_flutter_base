name: flutter_base
description: A new Flutter project.
version: 1.0.0+1
environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  get:
  get_storage:
  logger:
  sentry: ^6.4.0
  supercharged: ^2.0.0
  velocity_x: ^3.4.0
  connectivity_plus:
  graphql_flutter:
  jwt_decoder: ^2.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints:
  mockito:

flutter:
  uses-material-design: true
